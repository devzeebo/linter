{
  "name": "linter",
  "workspaces": [
    "packages/domain",
    "packages/engine",
    "packages/parser-typescript",
    "packages/reporter-console",
    "packages/rules-typescript"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "build:watch": "npm run build -- --watch",
    "test": "npm -w packages/engine run test"
  }
}